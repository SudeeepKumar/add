rules_version = '2';
// NOTE: Public read on individual invoices allows customers to view via shared link

service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }

    // Helper function to check if user owns the document
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // Transactions collection
    match /transactions/{transactionId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
      allow create: if isAuthenticated() && isOwner(request.resource.data.userId);
    }

    // Products collection
    match /products/{productId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
      allow create: if isAuthenticated() && isOwner(request.resource.data.userId);
    }

    // Invoices collection
    match /invoices/{invoiceId} {
      // Allow public read of individual invoices (for shared customer links)
      allow get: if true;
      // Listing all invoices requires authentication
      allow list: if isAuthenticated() && isOwner(resource.data.userId);
      allow write: if isAuthenticated() && isOwner(resource.data.userId);
      allow create: if isAuthenticated() && isOwner(request.resource.data.userId);
    }

    // Customers/Vendors collection (for ledger)
    match /customers/{customerId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
      allow create: if isAuthenticated() && isOwner(request.resource.data.userId);
    }

    // Suppliers collection
    match /suppliers/{supplierId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
      allow create: if isAuthenticated() && isOwner(request.resource.data.userId);
    }

    // Business Settings collection
    match /businessSettings/{userId} {
      // Allow public read (needed for public invoice page to show business info)
      allow read: if true;
      allow write: if isAuthenticated() && request.auth.uid == userId;
    }

    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
